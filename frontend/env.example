VITE_BACKEND_URL=http://localhost:3001
# Toggle the AI activity idea helper (set to false to hide the panel)
VITE_ACTIVITY_AI_ENABLED=true

# Show per-candidate matchmaking internals on the signed-in dashboard.
# Keep false in normal user-facing builds.
VITE_MATCHMAKING_DEBUG=false

